import React, {useState, useEffect} from 'react';
import {observer} from 'mobx-react-lite';
import {
  View,
  Text,
  ActivityIndicator,
  TouchableOpacity,
  StyleSheet,
  FlatList,
  Image,
} from 'react-native';

export default observer(function AllPosts() {
  return <View>{/** 전체 포스팅 조회 화면 */}</View>;
});

const styles = StyleSheet.create({});
